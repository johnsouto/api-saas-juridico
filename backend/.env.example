ENV=dev

# DB
DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgres:5432/juridico_db

# Auth/JWT
JWT_SECRET_KEY=change-me
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# CORS
CORS_ORIGINS=["http://localhost","https://localhost"]

# MinIO / S3
S3_ENDPOINT_URL=http://minio:9000
# URL pública (usada para presigned URLs no browser). Em dev: http://localhost:9000
S3_PUBLIC_ENDPOINT_URL=http://localhost:9000
S3_ACCESS_KEY=minio
S3_SECRET_KEY=minio123456
S3_BUCKET_NAME=juridico-files
S3_REGION=us-east-1
S3_USE_SSL=false
S3_VERIFY_SSL=false

# SMTP (opcional; se vazio, os emails são logados no console)
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
EMAIL_FROM=noreply@local
EMAIL_FROM_NAME=SaaS Juridico

# Cloudflare Turnstile (anti-bot) - optional (recommended in production)
# Create keys at: https://dash.cloudflare.com/turnstile
TURNSTILE_SECRET_KEY=

# Telegram alerts (optional)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

SEED_ON_STARTUP=true

# Platform admin (provisioning) - set a strong secret to enable /v1/platform/*
PLATFORM_ADMIN_KEY=

# Public URL (used in emails)
PUBLIC_APP_URL=http://localhost

# Billing
BILLING_PROVIDER=FAKE
# Optional shared secret for /api/v1/billing/webhook/* endpoints (recommended in production)
BILLING_WEBHOOK_SECRET=
